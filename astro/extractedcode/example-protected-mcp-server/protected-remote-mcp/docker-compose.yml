services:
  mcp-server:
    build:
      context: ./mcp-server
    environment:
      FUSIONAUTH_URL: ${FUSIONAUTH_URL}
      FUSIONAUTH_EXTERNAL_URL: ${FUSIONAUTH_EXTERNAL_URL}
      MCP_SERVER_URL: ${MCP_SERVER_URL}
    ports:
      - "8000:8000"
