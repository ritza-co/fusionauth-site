# MCP Server Environment Variables
# Copy this file to .env and fill in the values
# These values can be overridden by actual environment variables

# Base URL for the API (override if different from OpenAPI spec)
# CAUTION: Only use this for Development or Testing environments
#API_BASE_URL=http://localhost:9011

# Optional configureation to restrict which tools are exposed by the MCP server.
# This is useful to reduce the number of tokens.
# Default tool bucket prefixes. Anything else goes into other.
#TOOL_BUCKETS="create,delete,patch,update,retrieve,search"
# Comma-separated list of tool names to enable. If not set, all tools are enabled.
#USE_TOOLS="retrieve,search"

# API Authentication
API_KEY_APIKEYAUTH=your_api_key_here
BEARER_TOKEN_BEARERAUTH=your_bearer_token_here

# Add any other environment variables your API might need
